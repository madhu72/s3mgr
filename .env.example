# S3 Manager Environment Configuration
# Copy this file to .env and update with your values

# Server Configuration
PORT=8080
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Development Settings
GIN_MODE=debug

# Database Settings (BadgerDB will create a 'data' directory)
# No additional configuration needed for BadgerDB

# AWS Configuration (Set via web interface)
# These are configured through the application UI:
# - AWS_ACCESS_KEY_ID
# - AWS_SECRET_ACCESS_KEY  
# - AWS_REGION
# - S3_BUCKET_NAME
